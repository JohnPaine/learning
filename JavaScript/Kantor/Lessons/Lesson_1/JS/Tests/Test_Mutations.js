/**
 * Created by shtefan on 30.01.2016.
 */
describe("mutation", function () {

    //describe('Returns the remaining elements of an array after chopping off n elements from the head', function() {
    //    it('slasher([1, 2, 3], 2) should return [3]', function() {
    //        result = JSON.stringify(slasher([1, 2, 3], 2));
    //        assert(result == JSON.stringify([3]), 'slasher([1, 2, 3], 2) != [3]');
    //    });
    //    it('slasher([1, 2, 3], 0) should return [1, 2, 3]', function() {
    //        result = JSON.stringify(slasher([1, 2, 3], 0));
    //        assert(result == JSON.stringify([1, 2, 3]), 'slasher([1, 2, 3], 0) != [1, 2, 3]');
    //    });
    //    it('slasher([1, 2, 3], 9) should return []', function() {
    //        assert(JSON.stringify(slasher([1, 2, 3], 9)) == JSON.stringify([]), 'slasher([1, 2, 3], 9) != []');
    //    });
    //    it('slasher([1, 2, 3], 4) should return []', function() {
    //        assert(JSON.stringify(slasher([1, 2, 3], 4)) == JSON.stringify([]), 'slasher([1, 2, 3], 4) != []');
    //    });
    //});
});